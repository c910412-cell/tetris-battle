## 待定垃圾行點點的錨點。2026-09-22 起實際畫點的 X/Y 位置由 Battle.gd 每一幀
## 直接用目前 BoardAnchor 的即時大小/位置現算（對齊最下面那個可視行的正
## 中間、水平固定在盤面左邊 0.6 格的距離），不再讀這個節點自己的座標——所以
## 節點放在場景裡哪個位置都不影響實際點點位置，純粹給你一個編輯器裡的視覺
## 參考（配合子節點 Placeholder 的邊框）。
## 這個節點唯一真的會影響畫面的是下面這個 dot_size_ratio：點的實際像素大小
## = 目前格子大小 × 這個比例，所以你調完這個數字之後，之後不管怎麼放大縮小
## 盤面，點的大小都會自動跟著等比例縮放，不用每次重調。
class_name PendingDotsAnchor
extends Control

@export_range(0.1, 1.5, 0.05) var dot_size_ratio: float = 0.55
